# I want this to work, without MPF:

bundle agent main
{
    files:
        "/tmp/out.txt"
            overwrite => "Hello world";
}

# Other name suggestions:
# content, force_content, is_exactly, contains_only, overwrite_with
